default_install_hook_types: [pre-commit, pre-push]

repos:
  - repo: local
    hooks:
      - id: quality-gate
        name: nuggetizer quality gate
        entry: uv run python scripts/quality_gate.py
        language: system
        pass_filenames: false
        always_run: true
        stages: [pre-commit, pre-push]
